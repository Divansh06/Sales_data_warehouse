# 📊 Sales Data Warehouse & Reporting System

An **end-to-end Data Engineering project** that demonstrates how raw sales data can be transformed into a structured data warehouse and analyzed using SQL, with insights delivered through an interactive Streamlit dashboard.

This project showcases **ETL pipeline development**, **star schema modeling**, and **SQL-driven analytics**, similar to real-world data engineering workflows.

---

## 🚀 Project Overview

**Objective:**  
Build a complete data pipeline that ingests raw sales data, transforms it into analytics-ready tables, and enables business insights through SQL queries and dashboards.

**Key highlights:**
- End-to-end ETL pipeline using Python
- Fact and dimension table modeling (Star Schema)
- SQL-based analytics on a data warehouse
- Interactive dashboard using Streamlit

---

🏗️ Architecture 

Raw CSV data → Python ETL (Extract, Transform, Load) → SQLite Data Warehouse → SQL Analytics → Streamlit Dashboard

📂 Project Structure 

data/ (raw & processed data) → etl/ (extract, transform, load scripts) → sql/ (schema & analytics queries) → dashboard/ (Streamlit app) → config files (requirements.txt, .gitignore, README.md)
